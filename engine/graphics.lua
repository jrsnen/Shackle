



function graphicsettings(files, settings)
    files.configFileName = "engine/ogre.cfg"
    files.pluginsFileName = "engine/plugins.cfg"
    files.logFileName = "ogre.log"
    files.resourceFileName = "engine/resources.cfg"

    settings.title = "game"
    settings.customCapacities = ""
end

function graphicsNames(names)

end
